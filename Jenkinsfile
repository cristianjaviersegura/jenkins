pipeline {
  agent any
  stages {
      stage("Build") {
           steps {
               echo 'Build de app'
	         }
	    }		
       stage("Install") {
            steps {
                echo 'Install de app'
            }
      }  
         stage("Deploy") {
            steps {
                echo 'Deploy de app'
            } 
		      }
  }
  
}
  node {
    // Groovy script
  }
